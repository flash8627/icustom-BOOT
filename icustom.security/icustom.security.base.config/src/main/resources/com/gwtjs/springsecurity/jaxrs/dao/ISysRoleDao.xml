<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gwtjs.springsecurity.jaxrs.dao.ISysRoleDao">
	
	<select id="findRoleIdCount" resultType="int">
		SELECT max(T1.id)+1 FROM  sys_role T1
	</select>
	
	<insert id="insert">
		INSERT INTO sys_role (id, NAME,uid)
			VALUES (#{id,jdbcType=NUMERIC} , #{name,jdbcType=VARCHAR},#{userId,jdbcType=NUMERIC}) 
	</insert>
	
	<delete id="delete">
		DELETE
			FROM sys_role
			WHERE id = #{id,jdbcType=NUMERIC} 
	</delete>
	
	<update id="update">
		UPDATE sys_role
			SET 
			  NAME = #{name,jdbcType=VARCHAR} 
			WHERE id = #{id,jdbcType=NUMERIC}  
	</update>
	
	<select id="findRolePageCount" resultType="int">
		SELECT
			  COUNT(1) 
			FROM sys_role 
	</select>
	
	<select id="findRolePage" resultType="com.gwtjs.springsecurity.entity.SysRole">
		<include refid="select_base" />
	</select>
	
	<sql id="select_base">
		SELECT
			  id id,
			  NAME name,
			  uid uid 
			FROM sys_role
	</sql>
	
</mapper>