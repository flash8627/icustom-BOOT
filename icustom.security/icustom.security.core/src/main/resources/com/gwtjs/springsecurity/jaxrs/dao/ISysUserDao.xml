<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gwtjs.icustom.springsecurity.jaxrs.dao.ISysUserDao">

	<select id="findUserPage" resultType="com.gwtjs.icustom.springsecurity.entity.SysUserVO">
		<include refid="page_st" />
			<include refid="Base_Column_List" />
			<include refid="select_cond" />
		<include refid="page_et" />
	</select>
	
	<select id="findUserPageCount" resultType="int">
		SELECT count(*)
	  FROM sys_user_t su
	 WHERE su.VALID_FLAG = 1 
	 <include refid="select_cond" />
	</select>
	
	<select id="findByUsername" resultType="com.gwtjs.icustom.springsecurity.entity.SysUserVO">
		<include refid="Base_Column_List" /> 
		 AND upper(su.USER_NAME)=upper(#{username,jdbcType=VARCHAR})
	</select>
	
	<select id="findByAccount" resultType="com.gwtjs.icustom.springsecurity.entity.SysUserVO">
		<include refid="Base_Column_List" /> 
		 AND upper(su.USER_ACCOUNT)=upper(#{account,jdbcType=VARCHAR})
	</select>
	
	<sql id="select_cond">
		<if test='_parameter.get("user").account != null and _parameter.get("user").account != "" and _parameter.get("user").account != "null"'>
			AND upper(su.account) = upper(#{user.account,jdbcType=VARCHAR}) 
		</if>
		<if test='_parameter.get("user").username != null and _parameter.get("user").username != "" and _parameter.get("user").username != "null"'>
			AND upper(su.username) = upper(#{user.username,jdbcType=VARCHAR}) 
		</if>
	</sql>
		
	<sql id="Base_Column_List">
		SELECT USER_ID      id,
	       USER_ACCOUNT 	account,
	       USER_NAME    	username,
	       PASSWORD    		password,
	       EMAIL        	email,
	       ORDER_CODE   	orderCode
		FROM sys_user_t su
			WHERE su.VALID_FLAG = 1
	</sql>
	
	<sql id="page_st">
		SELECT * FROM (
		SELECT TT.* , ROWNUM RN FROM (
	</sql>
	
	<sql id="page_et">
		) TT
		<![CDATA[
		) WHERE RN<=#{page.endIndex} AND RN>=#{page.startIndex}
		]]>
	</sql>
	
</mapper>