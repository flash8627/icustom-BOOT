<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.gwtjs.springsecurity.jaxrs.dao.ISysResourceDao">
	
	<insert id="insert">
	INSERT INTO bootsample.sys_resource
            (id,
             method_name,
             method_path,
             remark,
             resource_id,
             resource_name,
             resource_string)
		VALUES (id,
		        method_name,
		        method_path,
		        remark,
		        resource_id,
		        resource_name,
		        resource_string)
	</insert>
	
	<delete id="delete">
		DELETE
			FROM sys_resource
			WHERE id = #{id,jdbcType=NUMERIC} 
	</delete>
	
	<update id="update">
		UPDATE bootsample.sys_resource
			SET 
			  method_name = method_name,
			  method_path = method_path,
			  remark = remark,
			  resource_id = resource_id,
			  resource_name = resource_name,
			  resource_string = resource_string
			WHERE id = id
	</update>
	
	<select id="findResourcePageCount" resultType="int">
		SELECT
			  COUNT(1) 
			FROM sys_resource  
			WHERE 1 = 1 
			<include refid="select_cond" />
	</select>
	
	<select id="findResourcePage" resultType="com.gwtjs.springsecurity.entity.SysResource">
		<include refid="select_base" />
			<include refid="select_cond" />
			LIMIT #{param2.startIndex} , #{param2.endIndex} 
	</select>
	
	<sql id="select_cond">
		<if test='_parameter.get("param1").methodName != null and _parameter.get("param1").methodName != ""'>
			AND UPPER(T1.method_name) LIKE UPPER('#{param1.methodName,jdbcType=VARCHAR}') 
		</if>
		<if test='_parameter.get("param1").resourceName != null and _parameter.get("param1").resourceName != ""'>
			AND UPPER(T1.resource_name) LIKE UPPER('#{param1.resourceName,jdbcType=VARCHAR}') 
		</if>
		
		<if test='_parameter.get("param1").resourceString != null and _parameter.get("param1").resourceString != ""'>
			AND UPPER(T1.resource_string) LIKE UPPER('#{param1.resourceString,jdbcType=VARCHAR}') 
		</if>
	</sql>
	
	<sql id="select_base">
		SELECT
			 id id,
             method_name methodName,
             method_path methodPath,
             remark remark,
             resource_id resourceId,
             resource_name resourceName,
             resource_string resourceString
			FROM sys_resource T1 
			WHERE 1 = 1 
	</sql>
	
</mapper>